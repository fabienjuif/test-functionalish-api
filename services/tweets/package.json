{
  "name": "@tt/service-tweets",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "start": "node .",
    "prettier": "prettier --check . --config ../../.prettierrc --ignore-path ../../.prettierignore",
    "buildSwc": "swc src -d dist --config-file ../../.swcrc",
    "clear": "rm -Rf node_modules dist .turbo *.log"
  },
  "dependencies": {
    "ramda": "^0.28.0",
    "@tt/lib-postgres": "^1.0.0",
    "@tt/lib-logger": "^1.0.0",
    "@tt/lib-cache": "^1.0.0"
  },
  "devDependencies": {
    "@types/ramda": "^0.28.11"
  }
}
